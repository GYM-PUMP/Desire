
$('textarea').val('')
// これでform_forで作成したtext_areaが空白になる。なのでbook_commentに影響するtext_areaは空白になるがもともと空白になっていいので問題ない。

$('#sampleModal-<%= @supplement.id %> #s-comments').append("<%= j(render partial: 'comments/supple_comment', locals: { comment: @supplement_comment}) %>");

$('#sampleModal-<%= @supplement.id %> #comments_count_show').html('コメント数：<%= @supplement.comments.count %>')